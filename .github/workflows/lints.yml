name: inception-rules

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check-rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash tests/check_inception_rules.sh
