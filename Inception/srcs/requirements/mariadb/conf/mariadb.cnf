[mysqld]
datadir         = /var/lib/mysql
socket          = /var/run/mysqld/mysqld.sock
pid-file        = /var/run/mysqld/mysqld.pid

# Accept connections from any IP — Docker network isolates who can reach port 3306
bind-address    = 0.0.0.0
port            = 3306

user            = mysql
